import React from "react";

export default function SOS() {
  return (
    <button
      style={{
        padding: "12px 20px",
        backgroundColor: "red",
        color: "white",
        border: "none",
        borderRadius: "6px",
        cursor: "pointer",
      }}
    >
      SOS
    </button>
  );
}
